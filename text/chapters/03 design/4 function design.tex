% Done
\section{Návrh funkcí aplikace}

% Review
V této sekci se budu věnovat podrobnější specifikaci funkcí navrhované aplikace. Nejprve se zaměřím na popis jednotlivých aktérů, kteří budou s aplikací interagovat. Následně na základě funkčních požadavků sepíšu seznam případů užití, které budou podrobněji specifikovat, jak mají jednotlivé části aplikace fungovat z pohledu uživatele. Nakonec na základě případů užití sepíšu podrobnou specifikaci vybraných funkcí v jazyce Gherkin. Tato specifikace umožní efektivní spouštění automatizovaných testů pomocí nástroje Cucumber a bude také sloužit jako dokumentace toho, jak se aplikace skutečně chová. Mimo jiné bude možné tuto specifikaci využít také jako zadání pro AI agenty, kteří mohou v budoucnu na základě této specifikace přesně implementovat požadované funkce, nebo například generovat dokumentaci a testy.

% Review
\subsection{Popis aktérů}

% Review
V rámci specifikace funkcí je třeba popsat tzv. aktéry, kteří reprezentují role, které mohou s aplikací interagovat a vykonávat v ní určité akce. Tito aktéři budou využiti jak v popisu případů užití, tak ve specifikaci funkcí pomocí jazyka Gherkin. Na základě požadavků aplikace jsem identifikoval následující aktéry.

% Review
\subsubsection{Uživatel}

% Review
Uživatel je osoba, která využívá aplikaci k učení se programování. Mezi jeho hlavní aktivity patří například zapisování si kurzů, učení se programování, interakce s ostatními uživateli a interakce s prvky gamifikace v aplikaci.

% Review
\subsubsection{Nepřihlášený uživatel}

% Review
Nepřihlášený uživatel je osoba, která v aplikaci není přihlášena a nemá tak přístup k většině funkcí aplikace. Může se však přihlásit nebo se registrovat v aplikaci.

% Review
\subsubsection{Premium uživatel}

% Review
Premium uživatel je osoba, která má aktivní předplatné nebo zkušební verzi předplatného. Premium uživatel může využívat všechny funkce jako uživatel. Kromě těchto akcí může vykonávat i další akce, ke kterým nemá běžný uživatel přístup.

% Review
\subsubsection{Korektor}

% Review
Korektor je osoba zodpovědná za kontrolu kvality výukového obsahu, aby byl správný a srozumitelný. V rámci aplikace může používat funkce podobně jako premium uživatel. Kromě toho může využívat podrobnější nahlašování chyb při učení.

% Review
\subsection{Specifikace případů užití}

% Review
Jako další krok při návrhu funkcí jsem se rozhodl sepsat případy užití. Případy užití představují způsob, jak specifikovat funkční chování systému z pohledu jeho aktérů. Každý případ užití popisuje konkrétní cíl aktéra a interakce se systémem, které k dosažení tohoto cíle vedou. Díky tomuto přístupu je tak možné detailněji formulovat požadavky a identifikovat okrajové případy, které je třeba při návrhu a implementaci ošetřit. Případy užití tak mohou být užitečné pro vývojáře, protože jim poskytnou detailnější popis toho, jak má daná část systému fungovat. Dále mohou být využity testery například jako podklad pro vytváření automatizovaných testů. Díky tomu, že bývají případy užití specifikovány pomocí přirozeného jazyka, mohou být čitelné i pro osoby, které nejsou technicky zaměřené. Celkově tak tvoří přesnější specifikaci systému, na základě které lze následně postupovat při návrhu a implementaci samotné aplikace. \cite{UseCases}

% Review
V průběhu let se vyvinulo několik způsobů, jak k případům užití přistupovat a jak je specifikovat. Základy techniky specifikace případů užití položil Ivar Jacobson v roce 1986. Alistair Cockburn \cite{Cockburn2001WritingEffectiveUseCases} následně popsal metodiku pro psaní podrobnějších případů užití. Přesto však prosazoval názor, že by forma specifikace případů užití měla odpovídat potřebám a rozsahu konkrétního projektu. Další techniku specifikace případů užití také popsal například Martin Fowler, který prosazoval názor, že neexistuje jeden standardní způsob, jak specifikovat případy užití, a že formát a rozsah případů užití by měl být zvolen dle potřeb konkrétního projektu. \cite[22-27]{NURUIDesignSteps}

% Review
V této práci jsem se tak rozhodl vycházet z práce Alistaira Cockburna \cite{Cockburn2001WritingEffectiveUseCases} a specifikovat případy užití způsobem, který bude užitečný pro vývoj této konkrétní aplikace. Pro každý případ užití tak budu specifikovat jeho identifikátor, název, popis, hlavního aktéra, hlavní scénář a případné alternativní scénáře. 

% Review
Pro specifikaci scénářů jsem se rozhodl využít jazyk Gherkin, který přináší několik zásadních výhod, kterým se budu věnovat v následující sekci. Jelikož se v jazyce Gherkin definují scénáře přímo v repozitáři aplikace v souborech s příponou \texttt{.feature} \cite{CucumberDocs}, rozhodl jsem se nejprve v rámci specifikace případů užití popsat hlavní a alternativní scénáře pouze stručným popisem a detailně je pak specifikovat přímo v repozitáři aplikace pomocí jazyka Gherkin. Díky tomu budu moci nejprve získat přehled o všech scénářích a okrajových případech a až poté provést jejich podrobnější specifikaci. Výsledkem tak bude využití jak výhod specifikace případů užití, tak i výhod jazyka Gherkin, kterému se budu věnovat v následující sekci.

% Review
V rámci specifikace případů užití jsem tak sestavil následující seznam případů užití.

% Done
\subsubsection{UC-1 Volba přihlášení nebo registrace}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si zobrazí úvodní stránku aplikace a zvolí, zda se chce přihlásit nebo registrovat. \\
  \textbf{Požadavek:} & \nameref{sec:fp-welcome-page} \\
  \textbf{Hlavní aktér:} & Nepřihlášený uživatel \\
  \textbf{Hlavní scénář:} & Nepřihlášený uživatel otevře aplikaci a aplikace mu zobrazí úvodní stránku. Uživatel zvolí možnost přihlášení a aplikace ho přesměruje na stránku přihlášení. \\
  \textbf{Alternativní scénář 1:} & Nepřihlášený uživatel otevře aplikaci a aplikace mu zobrazí úvodní stránku. Uživatel zvolí možnost registrace a aplikace ho přesměruje na stránku registrace. \\
  \textbf{Alternativní scénář 2:} & Přihlášený uživatel otevře aplikaci na úvodní stránce. Aplikace ho automaticky přesměruje na hlavní stránku v aplikaci. \\
\end{tabular}

% Done
\subsubsection{UC-2 Vyplnění úvodního dotazníku}

% Review
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel vyplní úvodní dotazník zaměřený na jeho cíle a zkušenosti s programováním. Aplikace uživateli na základě dotazníku doporučí kurzy a kariérní cesty, které si může zapsat. \\
  \textbf{Požadavek:} & \nameref{sec:fp-onboarding} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Po registraci uživatele aplikace zobrazí dotazník. Uživatel vyplní svoji úroveň programování, témata, která se chce učit a cíl, kterého chce dosáhnout. Aplikace zobrazí uživateli stránku, která ujistí uživatele, že daného cíle může s pomocí aplikace dosáhnout. Následně uživateli zobrazí seznam doporučených kurzů a kariérních cest. Uživatel vybere jeden z doporučených kurzů. Následně vyplní svůj denní cíl. Aplikace uživateli oznámí, že dotazník byl dokončen. Uživatel se pak může přesunout na hlavní stránku aplikace. \\
  \textbf{Alternativní scénář 1:} & Uživatel při průchodu hlavním scénářem nevybere kurz ze seznamu doporučených kurzů. Zvolí možnost pro zobrazení všech kurzů v aplikaci. Aplikace mu zobrazí přehled všech dostupných kurzů. Uživatel vybere jeden z kurzů a pokračuje dle hlavního scénáře vyplněním svého denního cíle. \\
  \textbf{Poznámka:} & Zapsání kariérní cesty se od zápisu kurzu nijak neliší a bude fungovat analogicky. \\
\end{tabular}

% Done
\subsubsection{UC-3 Zobrazení přehledu kurzů a kariérních cest}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Aplikace zobrazí uživateli stránku s přehledem kurzů a kariérních cest. Kliknutím na kurz nebo kariérní cestu se uživatel přesune na jeho resp. její stránku. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-browse} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Aplikace zobrazí uživateli stránku s přehledem kurzů a kariérních cest. Na této stránce aplikace zobrazí několik doporučených kurzů nebo kariérních cest a dále seznam všech dostupných kurzů a kariérních cest rozřazených podle kategorií. Uživatel se vybráním kurzu přesune na jeho stránku. \\
  \textbf{Alternativní scénář 1:} & Při průchodu hlavním scénářem uživatel nevybere kurz ale kariérní cestu a přesune se tak na stránku kariérní cesty. \\
\end{tabular}

% Done
\subsubsection{UC-4 Zobrazení přehledu doporučených kurzů a kariérních cest}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si může zobrazit přehled všech doporučených kurzů a kariérních cest. Kliknutím na kurz nebo kariérní cestu se pak přesune na jeho resp. její stránku. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-browse} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se ze stránky přehledu kurzů a kariérních cest přesune na stránku přehledu doporučených kurzů a kariérních cest. Aplikace mu zobrazí seznam všech doporučených kurzů a kariérních cest. Uživatel se vybráním kurzu přesune na jeho stránku. \\
  \textbf{Alternativní scénář 1:} & Při průchodu hlavním scénářem uživatel nevybere kurz ale kariérní cestu a přesune se tak na stránku kariérní cesty. \\
\end{tabular}

% Done
\subsubsection{UC-5 Vyplnění dotazníku pro úpravu doporučení kurzů a kariérních cest}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel na stránce přehledu doporučených kurzů a kariérních cest vyplní dotazník pro úpravu doporučení kurzů a kariérních cest. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-browse} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune ze stránky doporučených kurzů a kariérních cest na stránku dotazníku. V dotazníku vyplní jeho úroveň programování, témata, která se chce učit a cíl, kterého chce dosáhnout. Aplikace mu zobrazí informaci, že uživatel dokončil dotazník. Uživatel zvolí možnost zobrazení nových doporučení a přesune se tak zpět na stránku doporučených kurzů a kariérních cest. \\
\end{tabular}

% Done
\subsubsection{UC-6 Zobrazení stránky kurzu a zápis do kurzu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si zobrazí stránku kurzu a přihlásí se do něj. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-page} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune na stránku daného kurzu, aplikace mu zobrazí přehled kurzu včetně jeho popisu a přehledu částí kurzu. Uživatel zvolí možnost zapsání se do kurzu. Aplikace uživatele zapíše do kurzu a přesměruje ho na stránku učení první lekce kurzu. \\
\end{tabular}

% Done
\subsubsection{UC-7 Zobrazení stránky kurzu a pokračování v učení}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si zobrazí stránku kurzu a zvolí možnost pokračovat v učení. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-page} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune na stránku daného kurzu, aplikace mu zobrazí přehled kurzu včetně popisu a přehledu částí kurzu. Uživatel zvolí možnost pokračovat v učení. Aplikace přesměruje uživatele na stránku učení, kde mu zobrazí následující lekci, kterou by se měl uživatel v kurzu učit. \\
\end{tabular}

% Done
\subsubsection{UC-8 Spuštění lekce na stránce kurzu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel se ze stránky kurzu přesune na stránku kapitoly kurzu a spustí danou lekci. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-chapter-page} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se nachází na stránce daného kurzu, zvolí danou kapitolu a aplikace ho přesměruje na stránku kapitoly kurzu, kde dále zobrazí seznam lekcí rozdělených do sekcí. Uživatel vybere jednu z lekcí a aplikace ho přesměruje na stránku učení dané lekce. \\
  \textbf{Poznámka 1:} & Pokud uživatel ještě nemá kurz zapsaný a spustí lekci, aplikace ho automaticky zapíše do kurzu a následně ho přesměruje na stránku učení dané lekce. \\
  \textbf{Poznámka 2:} & Uživatel bude moci v kurzu spustit libovolnou lekci nehledě na to, které lekce již dříve dokončil.
\end{tabular}

% Done
\subsubsection{UC-9 Zobrazení certifikátu o dokončení kurzu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel s premium účtem si zobrazí stránku kurzu a zvolí možnost zobrazit certifikát o dokončení kurzu. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-certificate} \\
  \textbf{Hlavní aktér:} & Premium uživatel \\
  \textbf{Hlavní scénář:} & Premium uživatel, který dokončil daný kurz, se přesune na stránku daného kurzu a zvolí možnost zobrazit certifikát. Aplikace zobrazí uživateli stránku s certifikátem. \\
  \textbf{Alternativní scénář 1:} & Premium uživatel, který daný kurz ještě nedokončil, se přesune na stránku daného kurzu a zvolí možnost zobrazit certifikát. Aplikace zobrazí uživateli informaci, že musí nejprve dokončit kurz, aby mohl certifikát zobrazit. \\
  \textbf{Alternativní scénář 2:} & Uživatel, který nemá premium účet, se přesune na stránku daného kurzu a zvolí možnost zobrazit certifikát. Aplikace uživatele přesměruje na stránku s přehledem výhod premium účtu. \\
\end{tabular}

% Write - tohle je třeba implementovat (zatím jsem to nedělal)
\subsubsection{UC-10 Zobrazení stránky pro správu kurzů a odhlášení se z kurzu}

% Review
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si zobrazí stránku pro správu zapsaných kurzů a zvolí možnost odhlásit se z daného kurzu. \\
  \textbf{Požadavek:} & \nameref{sec:fp-course-management} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune na stránku pro správu zapsaných kurzů, aplikace mu zobrazí seznam zapsaných kurzů. Uživatel zvolí kurz, ze kterého se chce odhlásit. Aplikace uživateli zobrazí upozornění, že se daná akce nedá vrátit. Uživatel potvrdí akci odhlášení se z kurzu. Aplikace uživatele odhlásí z kurzu a přesměruje ho na stránku přehledu zapsaných kurzů. \\
\end{tabular}


% Done
\subsubsection{UC-10 Zobrazení stránky kariérní cesty a zápis do kariérní cesty}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si zobrazí stránku kariérní cesty a přihlásí se do ní. \\
  \textbf{Požadavek:} & \nameref{sec:fp-learning-paths} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune na stránku dané kariérní cesty, aplikace mu zobrazí přehled kariérní cesty včetně jejího popisu a přehledu kurzů, které jsou v kariérní cestě obsaženy. Uživatel zvolí možnost zapsání se do kariérní cesty. Aplikace uživatele zapíše do kariérní cesty a přesměruje ho na stránku učení první lekce prvního kurzu v rámci dané kariérní cesty. \\
\end{tabular}

% Done
\subsubsection{UC-11 Zobrazení stránky kariérní cesty a pokračování v učení}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel si zobrazí stránku kariérní cesty a zvolí možnost pokračovat v učení kariérní cesty. \\
  \textbf{Požadavek:} & \nameref{sec:fp-learning-paths} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune na stránku dané kariérní cesty, aplikace mu zobrazí přehled kariérní cesty včetně jejího popisu a přehledu kurzů, které jsou v kariérní cestě obsaženy. Uživatel zvolí možnost pokračovat v učení kariérní cesty. Aplikace přesměruje uživatele na stránku učení, kde mu zobrazí následující lekci, kterou by se měl uživatel učit v rámci dané kariérní cesty. \\
\end{tabular}

% Done
\subsubsection{UC-12 Zobrazení certifikátu o dokončení kariérní cesty}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel s premium účtem si zobrazí stránku kariérní cesty a zvolí možnost zobrazit certifikát o dokončení kariérní cesty. \\
  \textbf{Požadavek:} & \nameref{sec:fp-learning-paths} \\
  \textbf{Hlavní aktér:} & Premium uživatel \\
  \textbf{Hlavní scénář:} & Premium uživatel, který dokončil danou kariérní cestu, se přesune na stránku dané kariérní cesty a zvolí možnost zobrazit certifikát. Aplikace zobrazí uživateli stránku s certifikátem. \\
  \textbf{Alternativní scénář 1:} & Premium uživatel, který danou kariérní cestu ještě nedokončil, se přesune na stránku dané kariérní cesty a zvolí možnost zobrazit certifikát. Aplikace zobrazí uživateli informaci, že musí nejprve dokončit kariérní cestu, aby mohl certifikát zobrazit. \\
  \textbf{Alternativní scénář 2:} & Uživatel, který nemá premium účet, se přesune na stránku dané kariérní cesty a zvolí možnost zobrazit certifikát. Aplikace uživatele přesměruje na stránku s přehledem výhod premium účtu. \\
\end{tabular}

% Done
\subsubsection{UC-13 Zobrazení studijního plánu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Umožňuje uživateli si zobrazit stránku se svým studijním plánem, tedy přehledem všech zapsaných kurzů a jejich aktuálního režimu učení. \\
  \textbf{Požadavek:} & \nameref{sec:fp-study-plan} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel se přesune na stránku studijního plánu, aplikace mu zobrazí seznam zapsaných kurzů a jejich aktuální režimy učení. \\
\end{tabular}

% Done
\subsubsection{UC-14 Změna režimu učení kurzu ve studijním plánu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Umožňuje uživateli změnit režim učení jednotlivých kurzů ve studijním plánu. Uživatel může vybrat mezi režimy učení, opakování a pozastavení kurzu.  \\
  \textbf{Požadavek:} & \nameref{sec:fp-study-plan} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel na stránce studijního plánu změní u vybraného kurzu jeho režim na \uv{učení}, aplikace změnu uloží a kurz začne být zohledňován při doporučování nových lekcí k učení. \\
  \textbf{Alternativní scénář 1:} & Uživatel na stránce studijního plánu změní u vybraného kurzu režim na \uv{opakování}, aplikace změnu uloží a kurz začne být zohledňován při výběru látky k opakování. \\
  \textbf{Alternativní scénář 2:} & Uživatel na stránce studijního plánu změní u vybraného kurzu režim na \uv{pozastaveno}, aplikace změnu uloží a kurz přestane být zohledňován při doporučování lekcí k učení i k opakování. \\
\end{tabular}

% Done
\subsubsection{UC-15 Spuštění nadcházející lekce podle studijního plánu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Umožňuje uživateli spustit nadcházející doporučenou lekci podle jeho studijního plánu. \\
  \textbf{Požadavek:} & \nameref{sec:fp-study-plan-learning} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel otevře hlavní stránku, aplikace mu zobrazí nadcházející lekci podle studijního plánu, uživatel spustí tuto lekci a aplikace ho přesměruje na stránku učení. \\
  \textbf{Alternativní scénář 1:} & Uživatel otevře hlavní stránku, ale aplikace nenalezla žádnou nadcházející lekci, například protože má uživatel všechny lekce zapsaných kurzů dokončené. Aplikace zobrazí informaci, že jsou všechny lekce zapsaných kurzů dokončené a zobrazí možnost pro zapsání nových kurzů. Uživatel zvolí možnost pro zapsání nového kurzu a aplikace ho přesměruje na stránku pro výběr kurzů. \\
\end{tabular}

% Done
\subsubsection{UC-16 Spuštění opakování podle studijního plánu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Umožňuje uživateli spustit opakování látky, kterou aplikace doporučí na základě studijního plánu a toho, jak si uživatel naučenou látku pamatuje. \\
  \textbf{Požadavek:} & \nameref{sec:fp-study-plan-learning} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel otevře hlavní stránku aplikace a zvolí možnost opakování. Aplikace podle studijního plánu a historie učení vybere vhodnou látku k zopakování ze zapsaných kurzů, které jsou v režimu učení nebo opakování. Aplikace zobrazí informaci, ze kterého kurzu bude uživatel látku opakovat a dále zobrazí možnost spustit opakování. Uživatel spustí opakování a aplikace ho přesměruje na stránku učení. \\
  \textbf{Alternativní scénář 1:} & Uživatel otevře hlavní stránku aplikace a zvolí možnost opakování. Aplikace nenajde žádnou látku z opakování a zobrazí informaci, že všechna látka ze zapsaných kurzů byla již zopakována. \\
\end{tabular}

% Done
\subsubsection{UC-17 Spuštění učení jiné lekce než doporučené lekce podle studijního plánu}

% Done
\begin{tabular}{lp{0.7\linewidth}}
  \textbf{Popis:} & Uživatel se rozhodne učit se jinou lekci, než nadcházející lekci doporučenou dle studijního plánu. Spustí tak nadcházející lekci ze specifického kurzu. \\
  \textbf{Požadavek:} & \nameref{sec:fp-study-plan-learning} \\
  \textbf{Hlavní aktér:} & Uživatel \\
  \textbf{Hlavní scénář:} & Uživatel otevře hlavní stránku aplikace a zvolí možnost otevření studijního plánu. Aplikace mu zobrazí studijní plán, tedy seznam zapsaných kurzů a jejich aktuální režimy učení. U každého kurzu dále zobrazí možnost spustit učení se daného kurzu. Uživatel zvolí možnost učení se konkrétního kurzu a aplikace ho přesměruje na stránku učení, kde mu zobrazí látku z nadcházející lekce v rámci daného kurzu. \\
\end{tabular}


% Review
\subsection{Specifikace funkcí pomocí jazyka Gherkin}

% Review
Po sepsání případů užití jsem se rozhodl nové funkce přesněji specifikovat pomocí jazyka Gherkin \cite{CucumberDocs}. Gherkin je jednoduchý strukturovaný jazyk, který umožňuje pomocí přirozeného jazyka popsat chování systému v podobě textových scénářů. Díky přesně dané struktuře je následně možné tyto scénáře strojově zpracovat pomocí nástroje Cucumber\footnote{Dostupné na: \href{https://cucumber.io/}{https://cucumber.io/}} a ověřit, že se aplikace chová v souladu se specifikací pomocí automatizovaných testů. Celý proces je postavený na přístupu \textit{Behavior-Driven Development} (BDD) \cite{BDD}, tedy na metodice vývoje, při které se přesně specifikuje očekávané chování systému a následně se vytvoří automatizované testy, které ověří, že se aplikace skutečně podle specifikace chová. Tento přístup vznikl jako reakce na rozšířenou metodiku \textit{Test-Driven Development} (TDD) s cílem pomoci vývojářům a testerům nahlížet na testování více z pohledu samotného chování aplikace, přesněji pochopit a definovat požadované chování a minimalizovat tak nejednoznačnosti v zadání a implementaci jednotlivých funkcí \cite{BDDHistory}.

% Review
Tento přístup má několik zásadních výhod. Zaprvé samotná specifikace slouží jako přesná dokumentace toho, jak se současný systém skutečně chová. Díky přirozenému jazyku může být užitečná nejen testerům a vývojářům, ale i lidem, kteří nejsou technicky zaměření. Zadruhé díky automatizovaným testům, které jsou spouštěny dle jednotlivých scénářů, je možné průběžně ověřovat, že se aplikace skutečně chová přesně v souladu se specifikací. Zatřetí přesné specifikace funkcí mohou sloužit jako podklady pro asistenty umělé inteligence, kteří pomáhají vývojářům a testerům s implementací kódu. Používání nástrojů umělé inteligence tak může být díky přesné specifikaci a automatizovaným testům výrazně efektivnější. \cite{CucumberDocs}

% Review
Z výše zmíněných důvodů jsem se rozhodl využít jazyk Gherkin a nástroj Cucumber pro specifikaci funkcí a automatizaci testů s cílem zajistit efektivnější vývoj a kvalitnější implementaci samotných funkcí.

% Review
\subsubsection{Postup při specifikaci}

% Review
Funkce jsem specifikoval na základě definovaných případů užití a v souladu s dokumentací nástroje Cucumber \cite{CucumberDocs}, tedy přímo v repozitáři frontendu aplikace v souborech s příponou \texttt{.feature}. Tyto soubory jsem následně využil jako podklad pro implementaci a dále pro automatizaci testů, kterou jsem popsal v kapitole testování.

% Review
Soubory v jazyce Gherkin obsahují definici funkce \texttt{Feature} a jeden nebo více scénářů \texttt{Scenario} s jednotlivými kroky. Kroky scénáře jsou definovány pomocí klíčových slov \texttt{Given} a \texttt{When}, které definují předpoklady a dále 

% Review
Soubor ve formátu Gherkin typicky obsahuje definici funkce (\textit{Feature}), jeden nebo více scénářů (\textit{Scenario}) a jednotlivé kroky. Kroky jsou definovány pomocí klíčových slov, kterými jsou například slova \texttt{Given} definující předpoklady, \texttt{When} definující událost v systému, \texttt{Then} definující očekávaný výsledek a \texttt{And} umožňující definovat více kroků stejného typu. Příklad definice funkce pomocí jazyka Gherkin lze nalézt v ukázce kódu \ref{fig:feature-file-example}.

% Write - přidat actual ukázku z kódu
\begin{listing}[ht]
    \begin{minted}{gherkin}
Feature: Vyhodnocení odpovědi ve cvičení

  Scenario: Uživatel odpoví správně
    Given uživatel je přihlášen
    And má otevřené cvičení
    When odešle správnou odpověď
    Then aplikace zobrazí pozitivní zpětnou vazbu
    And přičte uživateli body
    \end{minted}
    % Write - přepsat název ukázky podle toho co to bude za ukázku
    \caption{Ukázka specifikace funkce pomocí jazyka Gherkin}
    \label{fig:feature-file-example}
\end{listing}

